#!/bin/bash
# Fake getent. Reads $FAKE_PASSWD (lines: user:x:uid:gid:GECOS:home:shell).
[[ "$1" == "passwd" ]] || exit 2
[[ -n "$2" ]] || { cat "$FAKE_PASSWD"; exit 0; }
line=$(grep -E "^$2:" "$FAKE_PASSWD" 2>/dev/null) || exit 2
[[ -n "$line" ]] || exit 2
printf '%s\n' "$line"
